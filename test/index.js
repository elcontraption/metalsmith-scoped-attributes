const Metalsmith = require('metalsmith');
const scopedAttributes = require('metalsmith-scoped-attributes');
const should = require('should');

describe('metalsmith-scoped-attributes', function () {

});